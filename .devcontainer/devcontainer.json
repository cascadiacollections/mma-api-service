{
  "name": "MMA API Service",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteUser": "vscode",
  "containerUser": "vscode",
  "updateRemoteUserUID": true,
  // No named volume for the uv cache: under rootless Podman the volume is
  // created root-owned and `uv sync` then fails with EACCES in
  // postCreateCommand. An image-owned cache directory works on every engine.
  "containerEnv": {
    "UV_LINK_MODE": "copy",
    "UV_CACHE_DIR": "/home/vscode/.cache/uv"
  },
  // Resolved against the real workspace folder, so a clone directory with
  // any name still puts the project venv first on PATH.
  "remoteEnv": {
    "PATH": "${containerWorkspaceFolder}/.venv/bin:${containerEnv:PATH}"
  },
  // Array (exec) form, not a string: Zed's built-in dev container support
  // wraps a string command in `/bin/sh -c` and then space-joins program and
  // args into a second `sh -c`, so `uv sync --frozen` reaches the shell as the
  // script `uv` with `sync`/`--frozen` as positional parameters and uv aborts
  // with "requires a subcommand". The array form survives that round-trip and
  // is also what the devcontainer CLI expects.
  "postCreateCommand": ["uv", "sync", "--frozen"],
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "MMA Pick'em",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "charliermarsh.ruff",
        "ms-python.python"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${containerWorkspaceFolder}/.venv/bin/python",
        "python.testing.pytestEnabled": true,
        "python.testing.pytestArgs": ["tests"],
        "editor.formatOnSave": true,
        "[python]": {
          "editor.defaultFormatter": "charliermarsh.ruff"
        }
      }
    }
  }
}
